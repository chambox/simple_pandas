from setuptools import setup, find_packages

setup(
    name="simple_pandas",  # Replace with your package's name
    version="0.2.0",  # Package version
    packages=find_packages(),  # This will find and include all packages automatically
)
